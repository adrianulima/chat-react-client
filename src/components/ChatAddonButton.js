import React from 'react'
import styles from '../styles.css'

const ChatAddonButton = (props) => {
  return <div className={styles['chat-addon-button']} {...props} />
}

export default ChatAddonButton

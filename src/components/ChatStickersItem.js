import React from 'react'
import styles from '../styles.css'

const ChatStickersItem = (props) => {
  return <div className={styles['chat-stickers-item']} {...props} />
}

export default ChatStickersItem

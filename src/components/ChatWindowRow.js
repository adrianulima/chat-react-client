import React from 'react'
import styles from '../styles.css'

const ChatWindowRow = (props) => (
  <div>
    <div className={styles['chat-window-row']} {...props} />
  </div>
)

export default ChatWindowRow

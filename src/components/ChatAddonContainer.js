import React from 'react'
import styles from '../styles.css'

const ChatAddonContainer = (props) => {
  return <div className={styles['chat-addon-container']} {...props} />
}

export default ChatAddonContainer
